#include <stdio.h>
#include <stdlib.h>

int main()
{
  float raioCirculo = 1;
  float area = 3.14159265359 * (raioCirculo * raioCirculo);
  printf("A area do circulo é %f", area);
  return 0;
}
